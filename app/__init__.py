"""app package initializer

This file makes the `app` directory an explicit package. No runtime code here.

Expose the Flask API instance as `api` so WSGI servers or other modules can import it
with `from app import api`.
"""
